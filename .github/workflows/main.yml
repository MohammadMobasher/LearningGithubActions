name: first workflow

on:
  push: 
    branches:
      - main

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: |
          echo 'Reason: ${{ github.event.inputs.reason }}'
    
